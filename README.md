# git-sync-all
A lightweight pair of scripts to recursively fetch, stash, and pull updates for all Git repositories within a directory. Perfect for developers and QA engineers managing dozens of repos / projects.
